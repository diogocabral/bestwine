<%= form_for(@grape) do |f| %>
  <% if @grape.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grape.errors.count, "error") %> prohibited this grape from being saved:</h2>

      <ul>
      <% @grape.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "Save", :class => 'btn btn-default' %>
    <%= link_to 'Back', grapes_path, :class => 'btn btn-default' %>
  </div>
<% end %>
