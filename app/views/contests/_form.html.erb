<%= form_for(@contest) do |f| %>
  <% if @contest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contest.errors.count, "error") %> prohibited this contest from being saved:</h2>

      <ul>
      <% @contest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => 'form-control' %> 
  </div>
  <div class="field">
    <%= f.label :starts_in %><br>
    <div class="input-group">
      <%= f.text_field :starts_in, :class => 'date form-control' %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit 'Salvar', :class => 'btn btn-default' %>
    <%= link_to 'Back', contests_path, :class => 'btn btn-default' %>
  </div>
<% end %>

<script type="text/javascript">
    $(function () {
        $('.date').datetimepicker();
    });
</script>